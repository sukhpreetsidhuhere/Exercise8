doctype html
html
  head
    title= title
  body
    if user
      h1 Yay node!
      p Hello Anonymous, #{user.displayName}!
      a(href="/auth/logout") Logout
    else
      h1 Yay node!
      p Hello Anonymous,
      a(href="/auth/google") Login
